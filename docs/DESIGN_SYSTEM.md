# Design System Token References

Design tokens are defined inside `lib/core/theme/app_theme.dart`.

---

## 🎨 Token Colors
- **Primary:** High-vibrancy brand blue (`AppColors.primary`).
- **Success:** Vibrant verification green (`AppColors.success`).
- **Surface:** Glassmorphic translucent cards (`AppColors.surface`).
- **Typography:** Standard `Inter` typography for readable alignment grids.
