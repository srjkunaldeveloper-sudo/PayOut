# Testing Architecture Guidelines

Payout utilizes standard testing structures to isolate unit logic from presentation layouts.

---

## 🧪 Testing Suites

- **Unit Tests (`test/unit/`):** Test models parsing and validator calculations.
- **Widget Tests (`test/widget/`):** Confirm UI states rendering in isolation.
- **Run Command:** Run `flutter test` to execute all tests.
